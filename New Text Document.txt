// ConsoleApplication11.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include "pch.h"
#include <iostream>
#include <string>
#include <fstream>

using namespace std;

struct kutije {
    string proizvod;
    int cijena_jediniènog, broj_proizvoda;
};



int main()
{
    kutije kutija[5];
    fstream file;
    int cijene[5];
    for(int i = 0;i<5;i++){

    file.open("c:\proba\kutije.txt");
    cout << "Unesi ime proizvoda: ";
    cin >> kutija[i].proizvod;
    file << kutija[i].proizvod << " ";

    cout << "Unesi broj proizvoda: ";
    cin >> kutija[i].broj_proizvoda;
    file << kutija[i].broj_proizvoda;

    cout << "Unesi cijenu jediniènog proizvoda: ";
    cin >> kutija[i].cijena_jediniènog;
    file << kutija[i].cijena_jediniènog;
    }
    file.close();

    cout << endl;

    for (int i = 0; i < 5; i++) {
        cout << kutija[i].proizvod << " ima ukupno cijenu: " <<
        kutija[i].broj_proizvoda * kutija[i].cijena_jediniènog << " kn." << endl;
        cijene[i] = kutija[i].broj_proizvoda * kutija[i].cijena_jediniènog;
    }


}